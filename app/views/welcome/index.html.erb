<div class="container">
  <div class="jumbotron" style="background-color: grey">
    <h1>Pair BnB</h1> 
    <p>A Place where you can ........</p> 
  </div>
 	<% if current_user == nil %>
 	<%else%>
 		<h1>MY Listing</h1>
		<p><%= link_to "New Listing", new_listing_path(user_id: current_user.id) %></p>
		<p><%= link_to "Edit Listing", edit_listing_path(current_user.id) %></p>
	<%end%>
	<%= render partial: "layouts/carousel"%>
</div>
</div>
